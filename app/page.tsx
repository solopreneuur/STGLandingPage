import Gate from "@/components/Gate";

export default function Home() {
  // Phase 0 skeleton: always renders the gate. Phase 4 wires the signed-cookie
  // branch here (cookie valid -> <SearchApp/>, otherwise -> <Gate/>).
  return <Gate />;
}
